이 프로젝트는 9600x 128G ram 에서 테스트 되었고
기존의 프로젝트는 AMX 명령어를 사용해서 제논이나 ROME 계열 CPU 만 지원을 하였습니다.
라이전 9000 시리즈에서는 AVX512를 지원을 하며 이를 대체해서 사용할 수 있겠다는 생각으로 접근하였습니다.
이 리포지토리는 작동한다는 것을 테스팅하기 위해서 실험적으로 만들어진것입니다.
기존의 원본 라이브러리에서 FP16버전으로 진행해도 작동가능할것으로 예상되나
소비자 등급의 CPU 에서는 FP16은 미지원이고 BF16 을 써야한다는 점 때문에 포팅하는 작업을 시작하였습니다.

테스트 모델 qwen3 coder 30b a3b bf16 - 허깅페이스의 safetensor 을 활용하였고
9600x 와 ram 5800Mhz와 cl46으로 소폭 오버클럭이 되어 있습니다.
memcpy 2GB:   27.014688 GB/sec 을 참고하시면 됩니다.

이를 감안해서 보길 바라고 
PP 는 60 TPS
TG 는 5.4 TPS

배치사이즈는 1이며 짧은 문장으로 테스트 했기 때문에 긴 컨텍스트에서는 성능이 점점 떨어진다는 점
답변 품질은 정상적으로 나오는것을 확인했습니다.
